services:
  - type: web
    name: invictus-plan-backend
    env: docker
    dockerfilePath: Dockerfile
    dockerContext: .
    # No startCommand needed - Dockerfile CMD is used
    # No buildCommand needed - Dockerfile handles the build
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /

